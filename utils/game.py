import random

class Board:
    def __init__(self, players, turn_count, active_cards, history_cards):
        self.player =  # that is a list of Player. It will contain all the players that are playing
        self.turn_count =  # that is an int
        self.active_card =  # that will contain the last card played by each player
        self.history_card =  # that will contain all the cards played since the start of the game, except for active_cards

    def __str__(self):  # its important to print the value not the position of the value.
        return "{self.breed} {self.animal}".format(self=self)

    def start_game()
        fill a deck


    def play()


print(The turn count)
print(The list of active cards.)
print(The number of cards in the history_cards)